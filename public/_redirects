# Netlify redirects file for React Router

# This ensures that all routes are handled by React Router on the client side

# Handle all client-side routes - MUST be at the end

/\* /index.html 200

# Optional: Redirect API calls to your backend (if using serverless functions)

# /api/\* https://your-backend-url.com/api/:splat 200
